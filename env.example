# Environment Configuration
NODE_ENV=development

# Server Configuration
PORT=4000
HOST=127.0.0.1
BASE_URL=http://localhost:4000
SERVICE_NAME=rudex

# Database Configuration
# Example local Postgres (adjust as needed)
DATABASE_URL=postgres://rudex:password@127.0.0.1:4020/rudex_dev
# JWT Configuration (RS256 asymmetric keys)
JWT_PRIVATE_KEY_PATH=./keys/private.pem
JWT_PUBLIC_KEY_PATH=./keys/public.pem

# Worker Configuration
WORKER_PORT=4010

# Redis Configuration
REDIS_URL=redis://127.0.0.1:4030

# SMTP Configuration (Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_FROM=pickpoker.fi@gmail.com
SMTP_USER=pickpoker.fi@gmail.com
SMTP_PASS="csto sbhn voqu oabw"


# External Service URLs
FRONTEND_URL=http://localhost:5173